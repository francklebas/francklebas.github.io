repos:
  - repo: local
    hooks:
      - id: llm-codereview
        name: LLM Code Review (Kimi / Gemini)
        entry: ./hooks/llm-codereview.sh
        language: system
        pass_filenames: true
        stages: [pre-commit]
